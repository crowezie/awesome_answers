<%# j is a rails helper method that escapes a string to make it work properly with Javascript, mainly by putting \ characters before all " characters"  %>

$("#new_answer").replaceWith("<%= j render 'form' %>");
